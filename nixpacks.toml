# Use npm install instead of npm ci to handle lockfile version differences
[phases.install]
cmds = ["npm install --production=false"]

# Disable CI=true so React build doesn't treat warnings as errors
[phases.build]
cmds = ["CI=false npm run build"]
